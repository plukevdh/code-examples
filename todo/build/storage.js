// Generated by CoffeeScript 1.6.3
(function() {
  var Storage;

  Storage = (function() {
    function Storage(key) {
      this.key = key != null ? key : "demo";
    }

    Storage.prototype.get = function(index) {
      var items;
      items = this.all();
      return items[index];
    };

    Storage.prototype.remove = function(index) {
      var items;
      items = this.all();
      items.splice(index, 1);
      return this.save(items);
    };

    Storage.prototype.all = function() {
      var raw_data;
      raw_data = localStorage[this.key];
      if (raw_data) {
        return this._fromJSON(raw_data);
      } else {
        return [];
      }
    };

    Storage.prototype.save = function(items) {
      localStorage.setItem(this.key, this._toJSON(items));
      return items;
    };

    Storage.prototype._toJSON = function(items) {
      return JSON.stringify(items);
    };

    Storage.prototype._fromJSON = function(json) {
      return JSON.parse(json);
    };

    return Storage;

  })();

  window.Storage = Storage;

}).call(this);

/*
//@ sourceMappingURL=storage.map
*/
